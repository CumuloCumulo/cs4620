import type { DetailedLectureTutorial, DetailedSlideBeat } from "./detailed-tutorial-data";

const book = (section: string, bridge: string) => ({ chapter: "教材第 13 章", section, href: "/book/13/", bridge });
const b = (page: number, title: string, change: string, explanation: string | string[], inspect: string[], extra: Partial<Omit<DetailedSlideBeat, "page" | "title" | "change" | "explanation" | "inspect">> = {}): DetailedSlideBeat => ({ page, title, change, explanation: Array.isArray(explanation) ? explanation : [explanation], inspect, ...extra });

export const monteCarlo: DetailedLectureTutorial = {
  pdf: "20monte-carlo.pdf",
  pageCount: 17,
  estimatedTime: "约 2-3 小时（含手算估计量与半球采样练习）",
  question: "渲染方程无法直接解析积分时，怎样把随机方向变成无偏、可收敛而且噪声尽量小的数值答案？",
  opening: [
    "上一讲最后得到半球光照积分，本讲立刻追问怎样计算它。老师没有从随机数 API 开始，而是先把积分改写成某个随机实验的期望值：只要单次样本贡献 g 的期望等于目标积分，多次独立样本的平均就能逼近答案。",
    "真正决定算法是否正确的不是样本看起来够不够随机，而是三件事：概率密度是否相对于正确的测度归一化、每个样本是否除以它实际使用的 PDF、PDF 为零的地方被积函数是否也为零。正确之后再谈方差；均匀、余弦比例和 BRDF 比例采样只是三种不同的方差选择。",
  ],
  outcomes: [
    "从小光源求和恢复半球渲染积分，并解释方向域的测度 dω",
    "区分功率、辐照度、辐亮度、角度和立体角的单位",
    "把一维积分写成 E[f(X)/p(X)]，证明估计量无偏",
    "区分离散概率质量与连续概率密度，正确解释 p(x)dx",
    "推导 N 个独立样本平均的标准差按 1/√N 下降",
    "为半球均匀、余弦比例和 BRDF 比例采样写出 PDF 与样本权重",
    "用重要性采样的支持集和方差条件判断一个采样策略是否有效",
  ],
  runningExample: {
    title: "贯穿例子：估计一块 Lambert 表面接收的环境光",
    description: "固定观察方向和反射率 ρ，在入射半球上抽取方向。我们会用同一个渲染积分比较 uniform hemisphere 与 cosine-weighted 两种估计量，并检查样本数翻四倍时噪声是否约减半。",
    code: "for i in 1..N:\n  wi, pdf = sampleDirection()\n  sum += Li(wi) * fr(wi,wo) * max(0,n·wi) / pdf\nLo = sum / N",
  },
  chapters: [
    {
      id: "illumination-integral",
      title: "先确认积分的域、单位和测度",
      pages: "1-5",
      purpose: "把上一讲的离散小光源求和变成半球积分，并补齐 Monte Carlo 采样所需的立体角概念。",
      beats: [
        b(1, "标题页：Monte Carlo Illumination", "Lecture 19 的材质模型进入数值积分阶段。", "本讲不再改变 BRDF，而是解决如何计算所有入射方向的总贡献。Monte Carlo 的核心不是模拟赌场，而是设计一个期望值等于目标积分的随机变量。", ["Lecture 20", "illumination 表明最终积分对象仍是光传输"], { pause: "先猜测：只随机选一个方向读取 Li，为什么通常不是正确的半球积分？" }),
        b(2, "从许多小光源求和：每项必须带立体角大小", "标题页加入小虫、球面小光源与离散求和。", ["第 k 个方向小片用亮度 Lₖ、方向 ωₖ 和大小 Ωₖ 表示。BRDF 与余弦决定该方向怎样贡献到 ωᵣ；Ωₖ 则告诉我们这片方向域占多大。若漏掉 Ωₖ，细分更多光源会无缘无故让总能量变大。", "这里的 Lₖ 是辐亮度，不是点光源强度；Ωₖ 是立体角，不是距离平方衰减。"], ["求和对象是方向小片", "每项含 Ωₖ、Lₖ、fᵣ 与余弦", "小虫无法区分连续环境和足够密的小光源"], { formula: "L_r(ω_r)=Σ_k Ω_k L_k f_r(ω_k,ω_r)|ω_k·n|" }),
        b(3, "取极限：小片亮度变成函数，大小变成 dω", "离散 Σ 与 Ωₖ 变为正半球上的积分和 dσ(ωᵢ)。", ["随着方向分区趋于无穷细，Lₖ 变成 Li(ωᵢ)，Ωₖ 变成立体角测度 dω。积分域 S²⁺ 是宏观法线上方的单位半球；不是对场景表面积分，也不是对二维屏幕积分。", "课件英文把 dσ 口述为 unit sphere 的 surface area。因为单位球上面积数值等于立体角，所以二者在这里一致；一般球半径 r 时要除以 r²。"], ["输出方向 ωᵣ 固定", "ωᵢ 是积分变量", "正半球只含 n·ωᵢ>0"], { formula: "L_r(ω_o)=∫_{S²⁺} L_i(ω_i)f_r(ω_i,ω_o)|n·ω_i|dω_i" }),
        b(4, "辐射度量单位复习：Monte Carlo 不会替你修复量纲", "渲染积分暂时退到 power、irradiance、radiance 三个单位。", "功率是单位时间能量 W；辐照度是单位面积入射功率 W/m²；辐亮度还按投影面积和立体角计量 W/(m²·sr)。积分中 Li·fᵣ·dω 的 sr 因子相消，输出仍是辐亮度。", ["irradiance 汇总方向", "radiance 保留方向", "BRDF 单位 1/sr 虽未写在页上却是单位闭合关键"], { formula: "[L_i][f_r][dω]=W/(m²·sr)·1/sr·sr=W/(m²·sr)", textbook: { chapter: "教材第 14 章", section: "14.6-14.8 辐射度量与光传输", href: "/book/14/", bridge: "教材从投影面积与方向锥定义辐亮度，并给出与本讲完全相同的半球渲染方程。" } }),
        b(5, "角度与立体角：2D 方向长度推广到 3D 方向面积", "单位页增加单位圆、单位球和 r、r² 的几何图。", ["平面角衡量单位圆上的弧长，整圆 2π rad；立体角衡量单位球上的面积，整球 4π sr，半球 2π sr。对于半径 r 的球面小片 A，Ω=A/r²。", "PDF 必须相对于同一个方向测度归一化。说‘半球均匀’意味着每单位立体角密度常数 1/(2π)，不是极角 θ 在 [0,π/2] 上均匀；后者会在极点和赤道分配错误。"], ["角度是 length/distance", "立体角是 area/distance²", "单位球使数值等于弧长或面积"], { formula: "full circle=2π rad；full sphere=4π sr；hemisphere=2π sr", pause: "若球面小灯面积为 0.04m²、距着色点 2m，它张开的近似立体角是多少？" }),
      ],
      checkpoint: "从第 2 页的 Ωₖ 求和推到第 3 页积分，并用单位解释为何半球均匀 PDF 是 1/(2π)。",
    },
    {
      id: "estimators-and-pdfs",
      title: "把积分变成期望：除以 PDF 才是算法的核心",
      pages: "6-11",
      purpose: "从均匀区间逐步建立估计量、概率空间、连续密度和一般期望公式。",
      beats: [
        b(6, "Monte Carlo 积分：寻找期望值等于答案的随机变量", "几何准备结束，页面写出一维积分 I 和估计量 g。", ["目标是 I=∫ₐᵇf(x)dx。我们选择某种随机 X，并构造 g(X)，使 E[g(X)]=I；g 是 estimator 的单样本贡献。多次运行实验并平均，不会改变期望，只会降低方差。", "无偏只说明长时间平均正确，不说明单帧好看，也不说明方差有限。"], ["随机的是 x，不一定是 f", "目标等式是 E[g]=I", "估计量和估计值要区分"], { formula: "I=∫ₐᵇf(x)dx，要求 E[g(X)]=I" }),
        b(7, "区间均匀采样：长度 (b-a) 是 1/pdf", "一般目标具体化为 X 在 [a,b] 均匀。", ["均匀密度 p=1/(b-a)，所以 E[f(X)]=(1/(b-a))∫f。为恢复积分，应令 g(X)=(b-a)f(X)=f(X)/p(X)。这就是一般 Monte Carlo 权重的第一个实例。", "若区间长度恰为 1，乘数看似消失，容易让人误以为随机函数值平均天然等于所有积分；换一个区间就能暴露错误。"], ["期望先给出函数平均", "积分等于区间长度乘平均", "(b-a) 不是经验缩放"], { formula: "X~Uniform[a,b]：p=1/(b-a)，g(X)=f(X)/p=(b-a)f(X)", example: { title: "手算 f(x)=x², [0,2]", lines: ["真值 I=∫₀²x²dx=8/3", "均匀样本 X 的 E[X²]=4/3", "乘区间长度 2：E[2X²]=8/3"] } }),
        b(8, "离散概率分布：先在两颗骰子上理解归一化与随机变量", "连续区间暂时换成 36 个等可能骰子结果。", ["样本空间 Ω={(i,j)} 有 36 个结果，每个概率 1/36，所有概率求和为 1。随机变量 S(i,j)=i+j 把结果映射到 2…12；S 的输出并不均匀，因为产生 7 的底层结果更多。", "这页的重要区分是 outcome、random variable 与其分布。渲染里底层均匀随机数经过方向变换后，方向的 PDF 也可能不均匀。"], ["概率分配给样本空间结果", "随机变量是样本空间上的函数", "归一化是 Σp=1"]),
        b(9, "连续概率密度：单点概率为零，p(x)dx 才有概率意义", "骰子换成 [0,6) 转盘与两个转盘之和。", ["连续分布中 p(x₀) 不是‘恰好得到 x₀ 的概率’，而是小区间概率除以宽度的极限：Pr(x₀<X<x₀+dx)≈p(x₀)dx。密度可以大于 1，只要对域积分为 1。", "两个均匀转盘之和形成三角形密度：靠近 6 的组合最多，靠近 0 或 12 的组合最少。这是卷积直觉，也再次说明变量变换会改变密度。"], ["连续归一化是 ∫Ωp(x)dx=1", "p(6)=1/6，端点密度为 0", "密度有单位，是相对于 dx 定义的"], { formula: "p(x₀)=lim_{dx→0} Pr(x₀<X<x₀+dx)/dx" }),
        b(10, "期望公式统一离散与连续情况", "概率例子收束为 E[f(X)] 的求和与积分定义。", ["离散时把每个函数值乘其概率后求和；连续时把 f(x)p(x) 对测度 dx 积分。第 7 页的均匀推导只是把 p=1/(b-a) 代入连续期望。", "期望对函数线性，这是多样本平均保持无偏的基础；独立性则是后面简单方差相加的条件。"], ["Σ 对应离散质量", "∫ 对应连续密度", "两式都用分布 p 加权"], { formula: "discrete: E[f(X)]=Σf(x)p(x)；continuous: E[f(X)]=∫f(x)p(x)dx" }),
        b(11, "重新看均匀采样：所谓均匀也是一个 PDF", "期望定义代回 [a,b]，复现 g=(b-a)f。", ["这页不是重复，而是把第 7 页的直觉正式纳入 PDF 框架：p=1/(b-a)，因此 E[g]=∫g p。要让它等于 ∫f，只能令 g=f/p。", "把‘采一个点’与‘该点代表多大区域’分开理解：1/p 正是样本代表的测度权重。非均匀采样会让不同点代表不同大小的区域。"], ["uniform 对应常数密度", "估计量仍是 f/p", "下一页将平均多个独立估计量"], { formula: "E[g(X)]=1/(b-a)∫ₐᵇg(x)dx；g=(b-a)f", textbook: book("13.2-13.3 连续概率与蒙特卡洛积分", "教材给出一般公式 ∫g=E[g/p]，并把 PDF 解释为样本在不同区域的代表权重。") }),
      ],
      checkpoint: "为 f(x)=x²、x∈[0,2] 分别写出均匀 PDF、单样本贡献和 N 样本估计量；证明其期望为 8/3。",
    },
    {
      id: "variance-and-importance",
      title: "正确只是起点：采样分布决定方差",
      pages: "12-14",
      purpose: "推导平方根收敛率，再把任意非零 PDF 的估计量应用到渲染积分。",
      beats: [
        b(12, "收敛率：独立样本平均的标准差是 σ/√N", "单样本估计量扩展为 N 个样本的平均 G_N。", ["若 g(Xᵢ) 独立同分布，和的方差是 Nσ²[g]；再除以 N²，得到平均的方差 σ²/N、标准差 σ/√N。课件中第一行下标混用 n/N，应统一理解为 N 个样本。", "误差减半通常需要样本数变为四倍；10 倍样本只把标准差降到约 0.316 倍。这解释了路径追踪噪声为何昂贵，也说明降低单样本方差往往比蛮力增样本更重要。"], ["结论依赖独立或足够弱相关", "方差按 1/N，标准差按 1/√N", "系统偏差不会随 N 增大消失"], { formula: "G_N=(1/N)Σg(X_i)；Var[G_N]=Var[g]/N；σ[G_N]=σ[g]/√N", example: { title: "噪声预算", lines: ["100 spp → 相对标准差尺度 1/10", "400 spp → 1/20，只有原来一半", "10000 spp → 1/100，是 100 spp 的 1/10"] } }),
        b(13, "非均匀采样：任何覆盖被积函数支持集的 PDF 都能无偏", "均匀 PDF 换成一般 p(x)，估计量显式写为 f/p。", ["若 X~p，则 E[f(X)/p(X)]=∫(f/p)p=∫f。关键条件不是 p 在整个数学域都非零，而是在 f(x)≠0 的地方必须 p(x)>0；否则某部分积分永远抽不到，产生偏差或除零。", "让 p 的形状接近 |f| 能让 f/p 更平坦，从而降低方差，这就是重要性采样。PDF 必须是可采样、可求值且相对于同一测度归一化。"], ["p 消掉是无偏证明", "支持集条件不能省略", "p 很小时 f/p 会产生 firefly 大权重"], { formula: "X~p：g(X)=f(X)/p(X)，E[g]=∫f；要求 f≠0⇒p>0", textbook: book("13.3 蒙特卡洛积分与重要性采样", "教材强调 g/p 的低方差目标、支持集条件和样本数四倍才能把误差约减半。"), pause: "若 f 在 [0,1] 恒正，而 p 只覆盖 [0,0.5]，增加再多样本能否修复答案？" }),
        b(14, "把一般估计量直接套入半球光照积分", "一维 f/p 抽象回到 Li·fᵣ·cos 的渲染被积函数。", ["选取 ωᵢ~p(ωᵢ)，单样本贡献就是 Li(ωᵢ)fᵣ(ωᵢ,ωᵣ)|n·ωᵢ|/p(ωᵢ)。其期望等于 Lᵣ；N 个样本取平均就是可执行算法。", "课件写 p>0 over whole hemisphere 是安全的充分条件。更精确的必要条件是被积函数非零处 p>0。注意 p 是每单位立体角密度；若用 (θ,φ) 参数采样，变量变换还必须包含 sinθ Jacobian。"], ["Li、BRDF、cosine 都在分子", "PDF 与 dω 使用同一测度", "最终还需除以样本数 N"], { formula: "L̂_r=(1/N)Σ_i [L_i(ω_i)f_r(ω_i,ω_o)|n·ω_i|/p(ω_i)]" }),
      ],
      checkpoint: "证明一般半球估计量无偏，并说明为什么 p 很小但分子很大的方向会造成 fireflies；算出样本数从 64 提到 1024 时标准差理论缩小多少倍。",
    },
    {
      id: "hemisphere-strategies",
      title: "三个半球 PDF：从能用到更匹配被积函数",
      pages: "15-17",
      purpose: "逐一代入均匀、余弦比例和 BRDF 比例 PDF，观察哪些因子被抵消、哪些方差仍然存在。",
      beats: [
        b(15, "半球均匀采样：PDF=1/(2π)，所有结构留在权重里", "一般 p(ωᵢ) 具体化为正半球常数密度。", ["正半球立体角为 2π，所以每单位立体角 PDF 是 1/(2π)。代入后单样本贡献乘 2π；方向概率完全不理会灯光、BRDF 和余弦，因此亮点或窄高光可能很少被命中，方差通常较高。", "正确生成半球均匀方向时 cosθ 应在 [0,1] 均匀，而不是 θ 均匀；可取 z=ξ₁、φ=2πξ₂，再由 x,y 构造单位向量。"], ["归一化来自半球面积 2π", "估计量中的余弦仍在", "uniform in solid angle 不等于 uniform θ"], { formula: "p(ω)=1/(2π)；g=2πL_i f_r|n·ω|", example: { title: "单位半球采样", lines: ["ξ₁,ξ₂~U[0,1)", "z=ξ₁，φ=2πξ₂", "r=√(1-z²)，local ω=(r cosφ,r sinφ,z)", "把局部 z 轴旋到表面法线 n"] } }),
        b(16, "余弦比例采样：PDF 吸收 Lambert 投影因子", "均匀策略改为 p(ω)=|n·ω|/π，估计量中的余弦消失。", ["因为 ∫hemisphere cosθdω=π，归一化 PDF 为 cosθ/π。代入 f/p 后余弦完全抵消，g=πLi fᵣ；样本更集中在接近法线、投影贡献大的方向。", "对 Lambert fᵣ=ρ/π，贡献进一步化为 ρLi。若环境 Li 是常数，所有样本贡献相同，方差为零；若有小而亮的光源，仍可能因没有匹配 Li 而噪声很大。"], ["π 来自余弦半球积分", "PDF 在地平线处为 0，但被积函数余弦也为 0", "适合漫反射，不一定适合窄镜面"], { formula: "p(ω)=|n·ω|/π；g=πL_i f_r；Lambert时 g=ρL_i", textbook: book("13.4.1 半球方向的分布转换", "教材给出从单位正方形生成 cosⁿθ 半球分布的方法；n=1 就是本页的余弦加权采样。") }),
        b(17, "BRDF 比例采样：抵消材质瓣，但仍未匹配 Li 与余弦", "最后把 PDF 设为 fᵣ/M(ωₒ)，其中 M 是对 BRDF 的半球积分。", ["固定 ωₒ 后令 p(ωᵢ)=fᵣ(ωᵢ,ωₒ)/M(ωₒ)，BRDF 因子在 f/p 中抵消，样本贡献变成 MLi|n·ωᵢ|。这能集中采样窄镜面瓣，但归一化 M 必须可知且抽样算法必须与实际 PDF 一致。", "它不是完整被积函数的最优分布：Li 可能集中在别处，余弦也未匹配。现代渲染常同时从灯光分布和 BSDF 分布采样，再用 multiple importance sampling 合并，避免任一策略独自漏掉重要区域。"], ["ωₒ 固定、ωᵢ 随机", "M 让 PDF 积分为 1", "抵消 fᵣ 后仍留下 Li 与 cosine"], { formula: "M(ω_o)=∫f_r(ω_i,ω_o)dω_i；p=f_r/M；g=M L_i|n·ω_i|", pause: "举例说明：镜面瓣很窄但唯一光源不在镜面方向时，纯 BRDF 采样会发生什么？" }),
      ],
      checkpoint: "为同一 Lambert 环境积分列出三种 PDF 和样本贡献；说明各自抵消了哪些被积函数因子，并选择一个高动态范围小光源场景解释为何需要同时采样光源与 BSDF。",
    },
  ],
  synthesis: [
    "测度层：方向积分使用立体角 dω；整球 4π sr、半球 2π sr，PDF 必须相对于同一测度归一化。",
    "物理层：Li·fᵣ·cosθ·dω 的单位闭合为出射辐亮度，随机估计不会改变物理量纲。",
    "概率层：连续 p(x) 是密度，p(x)dx 才是小区间概率；变量变换会改变 PDF。",
    "估计层：X~p 时用 f(X)/p(X)；只要覆盖被积函数支持集，其期望就是目标积分。",
    "收敛层：独立样本平均的方差按 1/N、标准差按 1/√N 下降；四倍样本通常只换来一半噪声。",
    "方差层：重要性采样让 p 接近被积函数幅度，使 f/p 更平坦；极小 PDF 会制造巨大权重和 fireflies。",
    "策略层：半球均匀匹配常数，余弦采样匹配投影因子，BRDF 采样匹配材质瓣；任何单一策略都可能漏掉 Li 的结构。",
  ],
  finalPractice: "实现 MonteCarloIlluminationLab：在同一 Lambert 球和可移动小面积光源上切换 uniform hemisphere、cosine-weighted、BRDF-weighted 三种采样。界面显示每个样本的 ω、PDF、分子、权重、累计均值与样本方差，并绘制 10、100、1000、10000 spp 的误差曲线。测试至少覆盖：每个 PDF 数值积分为 1、f≠0⇒p>0、f/p 无偏、半球均匀 1/(2π)、余弦积分 π、Lambert+常量环境在余弦采样下零方差、样本数四倍标准差约减半、错误地让 θ 均匀产生偏置，以及故意把求值 PDF 与生成 PDF 不一致时测试必须失败。",
};
